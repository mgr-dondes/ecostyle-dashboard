name: Daily Report
on:
  schedule:
      - cron: '5 5 * * 1-5'
        workflow_dispatch: {}
        jobs:
          report:
              runs-on: ubuntu-latest
                  steps:
                        - uses: actions/checkout@v4
                              - run: node send-report.js
